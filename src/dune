(install
 (section bin)
 (files timeout_linux.exe))

(executable
 (name timeout_linux)
 (libraries core core_extended yojson)
 (flags (:standard -g -bin-annot))
 (preprocess (pps ppx_sexp_conv ppx_let)))

; Local Variables:
; mode: tuareg-dune
; End:
